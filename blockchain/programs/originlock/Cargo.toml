[package]
name = "originlock"
version = "0.1.0"
description = "OriginLock — Decentralized IP ownership on Solana"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "originlock"

[features]
no-entrypoint = []
no-idl = []
no-log-ix = []
idl-build = ["anchor-lang/idl-build"]
cpi = ["no-entrypoint"]

[dependencies]
anchor-lang = "0.30.1"
